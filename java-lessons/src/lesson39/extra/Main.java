package lesson39.extra;
import lessonRunner.BaseLesson;

// LESSON 39 EXTRA - The user picks the pet while the program runs
// Keep "extends BaseLesson" below: without it, the lesson runner can't run this file.
//
// TASK:
// 1. Copy Animal, Dog and Cat into this folder, with  package lesson39.extra;
// 2. Import the Scanner and ask: "Would you like a pet? (1 = Dog, 2 = Cat): "
// 3. Declare  Animal animal;  but don't create it yet
// 4. if choice == 1:  animal = new Dog();     else:  animal = new Cat();
// 5. Call animal.speak() ONCE, after the if. Which speak() runs isn't decided until the
//    program is running and the user has answered: that's runtime polymorphism.
public class Main extends BaseLesson {
    public static void main(String[] args) {
    }
}

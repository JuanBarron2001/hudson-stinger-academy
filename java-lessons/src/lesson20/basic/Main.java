package lesson20.basic;
import lessonRunner.BaseLesson;

// LESSON 20 BASIC - Same name, different parameters
//
// TASK:
// 1. Write  static double add(double a, double b)  that returns a + b
// 2. Call add(1, 2) and print it. You should see 3.0 (it's a double).
// 3. Write a SECOND method with the exact same line, static double add(double a, double b),
//    and run it. Read the error, then delete the copy.
//    The name plus the parameter list is the method's SIGNATURE; no two methods can share one.
public class Main extends BaseLesson {
    public static void main(String[] args) {
    }
}

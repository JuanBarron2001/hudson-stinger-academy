package lesson36.extra;
import lessonRunner.BaseLesson;

// LESSON 36 EXTRA - Every shape knows its own area
//
// TASK:
// 1. Copy the four classes into this folder, with  package lesson36.extra;
// 2. Give each child its measurements and a constructor, and a real area():
//      Circle:     double radius              area = Math.PI * radius * radius
//      Triangle:   double base, height        area = 0.5 * base * height
//      Rectangle:  double length, width       area = length * width
// 3. In main, create  new Circle(3),  new Triangle(4, 5)  and  new Rectangle(6, 7)
// 4. Print each one's area(). You should get 28.27..., 10.0 and 42.0
public class Main extends BaseLesson {
    public static void main(String[] args) {
    }
}

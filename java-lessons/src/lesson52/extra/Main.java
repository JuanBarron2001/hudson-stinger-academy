package lesson52.extra;
import lessonRunner.BaseLesson;

// LESSON 52 EXTRA - Everything a HashMap can do
//
// TASK:
// 1. Build the map from the basic half, then put "Coconut" at 1.00
// 2. map.remove("Apple"), and print map.get("Coconut")
// 3. Print containsKey("Banana"), containsKey("Pineapple"), containsValue(1.0) and
//    containsValue(1). Why is the last one false? (Hint: 1 is an Integer, not a Double.)
// 4. if map.containsKey("Pineapple"), print its price; else print "Key not found"
// 5. Print map.size()
// 6. Print every pair with  for (String key : map.keySet())  as  key + ": $" + map.get(key)
public class Main extends BaseLesson {
    public static void main(String[] args) {
    }
}

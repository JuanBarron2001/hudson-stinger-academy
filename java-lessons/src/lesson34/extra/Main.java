package lesson34.extra;
import lessonRunner.BaseLesson;

// LESSON 34 EXTRA - The fish overrides move()
//
// TASK:
// 1. Copy the four classes into this folder, with  package lesson34.extra;
// 2. In Fish, write its own move() with @Override above it:
//      @Override
//      void move() { prints "This animal is swimming" }
// 3. In main, call move() on a Dog, a Cat and a Fish. Only the fish swims now.
// 4. Misspell the override as  void moves(),  keeping @Override, and read the error.
//    Without @Override, Java would quietly add a new method and the fish would run again.
//    Then fix the spelling.
public class Main extends BaseLesson {
    public static void main(String[] args) {
    }
}
